# Python API

所有公开 API 从 `eve_proxy_ng` 包顶层导出；编译扩展本体在 `eve_proxy_ng._core`。
每个条目都带中英双语 docstring（运行时 `help()` 与本页内容同源同步，
由 `tests/check_stubs.py` 守护）。

## 包总览

::: eve_proxy_ng

## 核心扩展

::: eve_proxy_ng._core

## CLI 包装器

::: eve_proxy_ng.cli
